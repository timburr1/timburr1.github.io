import{r as e,o as i,c as o,a as s,b as t,w as n,F as a,d as r,f as l}from"./app.554c1b80.js";const d={},c=s("h1",{id:"permissions-extended",tabindex:"-1"},[s("a",{class:"header-anchor",href:"#permissions-extended","aria-hidden":"true"},"#"),r(" Permissions (extended)")],-1),h=s("h2",{id:"discord-s-permission-system",tabindex:"-1"},[s("a",{class:"header-anchor",href:"#discord-s-permission-system","aria-hidden":"true"},"#"),r(" Discord's permission system")],-1),p=r("Discord permissions are stored in a 53-bit integer and calculated using bitwise operations. If you want to dive deeper into what's happening behind the curtains, check the "),m={href:"https://en.wikipedia.org/wiki/Bit_field",target:"_blank",rel:"noopener noreferrer"},u=r("Wikipedia"),g=r(" and "),f={href:"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Bitwise_Operators",target:"_blank",rel:"noopener noreferrer"},y=r("MDN"),b=r(" articles on the topic."),w=l('<p>In discord.js, permission bit fields are represented as either the decimal value of said bit field or its referenced flags. Every position in a permissions bit field represents one of these flags and its state (either referenced <code>1</code> or not referenced <code>0</code>).</p><p>Before we get into actually assigning permissions, let&#39;s quickly go over the method Discord uses to determine a guild member&#39;s final permissions:</p><ol><li>Take all permissions for all roles the guild member has and add them up.</li><li>Apply all denies for the default role (<code>@everyone</code>).</li><li>Apply all allows for the default role (<code>@everyone</code>).</li><li>Apply all denies for all additional roles the guild member has at once.</li><li>Apply all allows for all additional roles the guild member has at once.</li><li>Apply all denies for the specific guild member if they exist.</li><li>Apply all allows for the specific guild member if they exist.</li></ol><p>Due to this system, you cannot deny base permissions. If you grant <code>SEND_MESSAGES</code> to <code>@everyone</code> and don&#39;t grant it for a muted members role, muted members will still be able to send messages unless you specify channel-based overwrites.</p><p>All additional roles allow overwrites are applied after all additional roles denies! If any of a member&#39;s roles have an overwrite to allow a permission explicitly, the member can execute the associated actions in this channel regardless of the role hierarchy.</p><p>Placing an overwrite to allow <code>SEND_MESSAGES</code> on a role will result in members with this role not being mutable via role assignment in this channel.</p><h2 id="elevated-permissions" tabindex="-1"><a class="header-anchor" href="#elevated-permissions" aria-hidden="true">#</a> Elevated permissions</h2>',7),A=r("If the guild owner enables the server's two-factor authentication option, everyone executing a specific subset of actions will need to have 2FA enabled on their account. As bots do not have 2FA themselves, you, as the application owner, will need to enable it on your account for your bot to work on those servers. Check out "),v={href:"https://support.discord.com/hc/en-us/articles/219576828-Setting-up-Two-Factor-Authentication",target:"_blank",rel:"noopener noreferrer"},E=r("Discord's help article"),I=r(" if you need assistance with this."),S=l('<p>The permissions assigned to these actions are called &quot;elevated permissions&quot; and are: <code>KICK_MEMBERS</code>, <code>BAN_MEMBERS</code>, <code>ADMINISTRATOR</code>, <code>MANAGE_CHANNELS</code>, <code>MANAGE_GUILD</code>, <code>MANAGE_MESSAGES</code>, <code>MANAGE_ROLES</code>, <code>MANAGE_WEBHOOKS</code> and <code>MANAGE_EMOJIS</code>.</p><h2 id="implicit-permissions" tabindex="-1"><a class="header-anchor" href="#implicit-permissions" aria-hidden="true">#</a> Implicit permissions</h2><p>Some Discord permissions apply implicitly based on logical use, which can cause unwanted behavior if you are not aware of this fact.</p><p>The prime example for implicit permissions is <code>VIEW_CHANNEL</code>. If this flag is missing in the final permissions, you can&#39;t do anything on that channel. It makes sense, right? If you can&#39;t view the channel, you can&#39;t read or send messages in it, set the topic, or change its name. The library does not handle implicit permissions for you, so understanding how the system works is vital for you as a bot developer.</p>',4),N=r("Let's say you want to send a message to a channel. To prevent unnecessary API calls, you want to make sure your bot's permissions in this channel include "),M=s("code",null,"SEND_MESSAGES",-1),_=r(" (more on how to achieve this "),x=r("here"),k=r("). The check passes, but you still can't send the message and are greeted with "),D=s("code",null,"DiscordAPIError: Missing Access",-1),R=r("."),T=l('<p>This error means your bot is missing <code>VIEW_CHANNEL</code>, and as such, can&#39;t send messages either.</p><p>One possible scenario causing this: the channel has permission overwrites for the default role <code>@everyone</code> to grant <code>SEND_MESSAGES</code> so everyone who can see the channel can also write in it, but at the same time has an overwrite to deny <code>VIEW_CHANNEL</code> to make it only accessible to a subset of members.</p><p>As you only check for <code>SEND_MESSAGES</code>, the bot will try to execute the send, but since <code>VIEW_CHANNEL</code> is missing, the API denies the request.</p><div class="custom-container tip"><p class="custom-container-title">TIP</p><p>Causes for &quot;Missing Access&quot;:</p><ul><li>Text Channels require <code>VIEW_CHANNEL</code> as detailed above.</li><li>Voice Channels require <code>CONNECT</code> in the same way.</li><li>Reacting to a message requires <code>READ_MESSAGE_HISTORY</code> in the channel the message was sent.</li><li>When deploying slash commands: Enable the <code>applications.commands</code> scope (for more information see the <a href="/preparations/adding-your-bot-to-servers">adding your bot</a> section).</li></ul></div><h2 id="limitations-and-oddities" tabindex="-1"><a class="header-anchor" href="#limitations-and-oddities" aria-hidden="true">#</a> Limitations and oddities</h2><ul><li>Your bot needs <code>MANAGE_ROLES</code> in its base permissions to change base permissions.</li><li>It needs <code>MANAGE_ROLES</code> in its final permissions to change permission overwrites.</li><li>It cannot edit permissions for roles that are higher than or equal to its highest role.</li><li>It cannot grant permissions it doesn&#39;t have.</li><li>It can manage overwrites for roles or users with higher roles than its own highest role.</li><li>It can manage overwrites for permissions it doesn&#39;t have.</li><li>Members with the <code>ADMINISTRATOR</code> permission are not affected by overwrites at all.</li></ul><h2 id="missing-permissions" tabindex="-1"><a class="header-anchor" href="#missing-permissions" aria-hidden="true">#</a> Missing permissions</h2><p>During your development, you will likely run into <code>DiscordAPIError: Missing Permissions</code> at some point. One of the following can cause this error:</p><ul><li>Your bot is missing the needed permission to execute this action in its calculated base or final permissions (requirement changes based on the type of action you are trying to perform).</li><li>You provided an invalid permission number while trying to create overwrites. (The calculator on the apps page returns decimal values while the developer documentation lists the flags in hex. Make sure you are not mixing the two and don&#39;t use the hex prefix <code>0x</code> where not applicable)</li><li>It is trying to execute an action on a guild member with a role higher than or equal to your bot&#39;s highest role.</li><li>It is trying to modify or assign a role higher than or equal to its highest role.</li><li>It is trying to add a managed role to a member.</li><li>It is trying to remove a managed role from a member.</li><li>It is trying to execute a forbidden action on the server owner.</li><li>It is trying to execute an action based on another unfulfilled factor (for example, reserved for partnered guilds).</li><li>It is trying to execute an action on a voice channel without the <code>VIEW_CHANNEL</code> permission.</li><li>It is trying to create a channel or channel overwrite including the <code>MANAGE_ROLES</code> flag but does not have the <code>ADMINISTRATOR</code> permission or an explicit <code>MANAGE_ROLES</code> overwrite on this channel (note that the global permission does not count)</li></ul><div class="custom-container warning"><p class="custom-container-title">WARNING</p><p>Granting the <code>ADMINISTRATOR</code> permission does not skip any hierarchical check!</p></div>',10);d.render=function(r,l){const d=e("OutboundLink"),G=e("RouterLink");return i(),o(a,null,[c,h,s("p",null,[p,s("a",m,[u,t(d)]),g,s("a",f,[y,t(d)]),b]),w,s("p",null,[A,s("a",v,[E,t(d)]),I]),S,s("p",null,[N,M,_,t(G,{to:"/popular-topics/permissions.html#checking-for-permissions"},{default:n((()=>[x])),_:1}),k,D,R]),T],64)};export{d as default};
